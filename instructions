1. Deactivate old twitter. Sign into dev account in a different/non-default browser
2.Delete old app.

(I needed to start over because I need to generate a new Auth URL to enable the non-primary account)
